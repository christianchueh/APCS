import streamlit as st

st.set_page_config(page_title="第六課：while迴圈與特定條件實戰", page_icon="🔄", layout="wide")

st.title("🔄 第六課：while 迴圈與特定條件實戰特訓")
st.caption("授課教師：闕河正 老師")
st.markdown("---")

st.info("""
🎯 **課程目標**：掌握「非固定次數」的迴圈核心——`while` 迴圈、精通 `break` 與 `continue` 的跳轉機制，並透過進位轉換與 ZeroJudge 三大經典題，高強度鍛鍊條件控制與位元邏輯。
""")

# ==========================================
# 核心心法：while 迴圈的核心觀念
# ==========================================
st.header("🎯 闕老師開講：什麼時候該用 while 迴圈？")
st.markdown("""
在前面幾課中，我們學到的 `for` 迴圈非常適合用在**「已知執行次數」**（例如：跑 10 次、走訪整個陣列）的場景。
然而，在實際開發或 APCS 考題中，有許多狀況是**「不知道要跑幾次，只知道某個特定條件成立時就要繼續跑」**，這時候就是 `while` 迴圈大顯身手的時候了！
""")

st.subheader("💡 1. 核心語法結構")
st.code("""
while 條件式:
    # 當條件式為 True 時，會重複執行這裡的程式碼
    # 記得要在迴圈內改變條件相關變數，否則會變成「無窮迴圈」死機！
""", language="python")

st.subheader("📖 經典範例：輸入數字 n，從 n 開始倒數到 0")
st.markdown("這是一個最標準的非固定次數應用，學生輸入多少，我們就倒數幾次：")
st.code("""
n = int(input("請輸入一個正整數："))
while n >= 0:
    print(n, end=" ")
    n -= 1  # 👈 關鍵！每次都要讓 n 減少，否則會陷入無窮迴圈
print("\\n倒數結束！")
""", language="python")

st.markdown("---")

# ==========================================
# 經典演算法：進位轉換與 while 追蹤
# ==========================================
st.header("🧮 核心演算法：十進制轉二進制（進位轉換）")
st.markdown("""
**觀念引導**：將一個十進制數字轉為二進制，方法是**「將被除數重複除以 2，並記錄餘數，直到被除數（商）變成 0 為止」**。
因為我們根本不知道學生輸入的數字需要除以幾次才會歸零，所以這必須使用 `while` 迴圈！
""")

# 動態追蹤表範例
st.markdown("#### 🔍 人肉電腦變數追蹤表（以輸入 `n = 13` 為例）")
st.markdown("流程：`餘數 = n % 2` ➔ `n = n // 2` (重複至 n == 0)，最後將餘數**由下往上（反向）**排列。")

st.table([
    {"步驟": "初始狀態", "目前被除數 n 的值": "13", "計算餘數 (n % 2)": "無", "下一輪的商 (n // 2)": "13"},
    {"步驟": "第一輪迴圈", "目前被除數 n 的值": "13", "計算餘數 (13 % 2)": "1 👉 (最後一位數)", "下一輪的商 (13 // 2)": "6"},
    {"步驟": "第二輪迴圈", "目前被除數 n 的值": "6", "計算餘數 (6 % 2)": "0", "下一輪的商 (6 // 2)": "3"},
    {"步驟": "第三輪迴圈", "目前被除數 n 的值": "3", "計算餘數 (3 % 2)": "1", "下一輪的商 (3 // 2)": "1"},
    {"步驟": "第四輪迴圈", "進來時 n = 1", "計算餘數 (1 % 2)": "1 👉 (第一位數)", "下一輪的商 (1 // 2)": "0 ⭐ 觸發結束條件"},
])
st.success("將餘數反過來排列，得到 13 的二進制結果為：`1101`")

with st.expander("👀 點擊查看「十進制轉二進制」範例程式碼"):
    st.code("""
n = int(input())
if n == 0:
    print(0)
else:
    result = ""
    while n > 0:
        remainder = n % 2
        result = str(remainder) + result  # 👈 巧妙技巧：直接把新餘數加在最前面，就不用最後再反轉了！
        n = n // 2
    print(result)
""", language="python")

st.markdown("---")

# ==========================================
# 控制字：continue 與 break
# ==========================================
st.header("🛑 迴圈兩大護法：break 與 continue")
st.markdown("這兩個控制字是操控迴圈動向的最高權力，無論是 `for` 還是 `while` 都適用：")

col1, col2 = st.columns(2)
with col1:
    st.markdown("### 💥 `break`（直接炸毀迴圈）")
    st.error("只要遇到 `break`，**整個迴圈立刻解散**，後面還沒跑的圈數全部取消，直接跳出迴圈外。")
    st.code("""
# 範例：找出 1~10 中第一個被 7 整除的數
for i in range(1, 11):
    if i % 7 == 0:
        print("找到了！是", i)
        break  # 👈 後面的 8, 9, 10 都不用跑了，直接收工
""", language="python")

with col2:
    st.markdown("### 🏃 `continue`（這圈不跑了，跳下一圈）")
    st.warning("遇到 `continue` 時，**只有「當下這一圈」後面還沒寫的程式會被略過**，但迴圈沒有解散，會立刻去跑「下一圈」。")
    st.code("""
# 範例：印出 1~5，但跳過 3
for i in range(1, 6):
    if i == 3:
        continue  # 👈 看到 3 直接跳去下一圈(i=4)，底下的 print 不會執行
    print(i, end=" ")
# 輸出結果：1 2 4 5
""", language="python")

st.markdown("---")

# ==========================================
# 三大實作練習（ZeroJudge 戰場）
# ==========================================
st.header("🔥 第六課實戰戰場：ZeroJudge 經典三大挑戰")
st.write("請同學們點擊下方連結前往題目頁面，並運用本課學到的 `while` 技巧與位元概念嘗試解題：")

zj1, zj2, zj3 = st.tabs(["🎯 挑戰 1：a132 - Parity", "🎯 挑戰 2：a414 - 位元運算之進位篇", "🎯 挑戰 3：a038 - 數字翻轉"])

with zj1:
    st.markdown("### 🔗 [ZeroJudge a132: Parity](https://zerojudge.tw/ShowProblem?problemid=a132)")
    st.markdown("""
    * **題目重點**：輸入一個十進制整數 $I$，請輸出它的二進制表示法，並計算其中包含了幾個 `1`（這稱為奇偶校驗位 Parity）。當輸入 `0` 時代表程式結束。
    * **闕老師破題密碼**：
      1. 這題是標準的「進位轉換變形」。你需要用 `while n > 0` 的結構不斷 `% 2` 與 `// 2`。
      2. 在除的過程中，只要發現 `餘數 == 1`，計數器就 `+1`。
      3. 記得要處理輸入多筆測資，直到讀到 `0` 終止的邏輯。
    """)

with zj2:
    st.markdown("### 🔗 [ZeroJudge a414: 位元運算之進位篇](https://zerojudge.tw/ShowProblem?problemid=a414)")
    st.markdown("""
    * **題目重點**：輸入一個十進制正整數 $N$，求將 $N$ 加上 $1$ 之後，在二進制的世界中會發生「連續幾次的進位」？當輸入 `0` 時結束。
    * **闕老師破題密碼**：
      * **絕對不要**真的把數字轉成字串去加 1，那樣在大量數據時會造成時效超限（TLE）！
      * 想想看二進制的加法：`1 + 1 = 10`（進位）、`11 + 1 = 100`（連續進位 2 次）。
      * 規律：**只要最低位元是 1，加 1 就一定會引發進位，且該位元變 0**。因此題目其實就是問：**「這個數的二進制末尾，連續有幾個 1 ？」**
      * 利用 `while N % 2 == 1:` 檢查最後一位是不是 1。如果是，進位數 `+1`，然後 `N //= 2` 砍掉最後一位，繼續看下一位，直到遇到 0 為止！
    """)

with zj3:
    st.markdown("### 🔗 [ZeroJudge a038: 數字翻轉](https://zerojudge.tw/ShowProblem?problemid=a038)")
    st.markdown("""
    * **題目重點**：輸入一個整數，將它顛倒過來輸出。例如輸入 `1234` 輸出 `4321`；輸入 `12300` 則輸出 `321`（前面的 0 要自動消失）。
    * **闕老師破題密碼**：
      * 寫法一（純數學解）：利用 `while n > 0` 每次 `% 10` 取出最後一位，然後舊的答案 `* 10` 加上新位數，最後 `n //= 10`。這種寫法會自然脫落字頭零。
      * 寫法二（字串偷編法）：利用 Python 超強的字串切片 `[::-1]` 來翻轉，但翻轉後要用 `int()` 將其轉回整數，這樣前面的 `00` 就會自動被消掉！
    """)

# ==========================================
# 老師後台專區（答案卡）
# ==========================================
with st.sidebar.expander("🔑 闕老師後台：ZeroJudge 三題標準解法
